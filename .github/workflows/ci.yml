name: CI

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_call:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Worker dependencies
        run: npm ci --prefix backend/worker

      - name: Verify ONNX models
        run: pnpm run verify:onnx-models

      - name: Typecheck
        run: pnpm run typecheck

      - name: Typecheck Worker
        run: pnpm run backend:typecheck

      - name: Svelte check
        run: pnpm run check

      - name: Lint
        run: pnpm run lint

      - name: Format check
        run: pnpm run format:check

      - name: Dead code audit
        run: pnpm run audit:deadcode

      - name: Dependency vulnerability audit
        run: pnpm audit --prod --audit-level high

      - name: Test
        run: pnpm run test

      - name: Backend tests
        run: pnpm run backend:test

      - name: Build
        run: pnpm run build

  # No Linux E2E job: the app relies on Windows-only native modules (DXGI screen
  # capture), so the full Electron app can't boot on Linux. E2E runs on Windows
  # below; Linux still covers typecheck/lint/unit/build in `validate`.

  validate-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify Windows main process
        run: |
          pnpm run verify:onnx-models
          pnpm run typecheck:main
          pnpm run test:main
          pnpm run build

      - name: E2E smoke tests (Windows)
        run: pnpm exec playwright test --config playwright.config.ts
