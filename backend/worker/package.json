{
	"name": "worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy --env=\"\"",
		"dau": "node scripts/dau.mjs",
		"dev": "wrangler dev --env dev",
		"start": "wrangler dev --env dev",
		"test": "vitest",
		"test:smoke": "vitest run --config vitest.smoke.mts",
		"typecheck": "tsc --noEmit",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"@types/node": "^25.3.3",
		"typescript": "^5.5.2",
		"vitest": "~5.0.0",
		"wrangler": "^4.70.0"
	},
	"overrides": {
		"esbuild": "^0.28.1",
		"sharp": "^0.35.2",
		"undici": "^7.29.0",
		"ws": "^8.21.0"
	}
}
