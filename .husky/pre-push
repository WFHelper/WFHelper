# same checks as CI so failures show up before the push, not after.
# worker tests + packaging stay CI-only (heavy). bypass: git push --no-verify
set -e
pnpm typecheck
pnpm run check
pnpm lint
pnpm run audit:deadcode
pnpm test
